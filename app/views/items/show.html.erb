<%= link_to "View original (in new window)", @item.url, target: "_blank" %>

<% if @item.body.blank? %>
  <i>No content parsed.</i><%= link_to "Try again", item_parse_path(item_id: @item.id) %>
<% else %>
  <%= @item.body.html_safe %>
<% end %>
